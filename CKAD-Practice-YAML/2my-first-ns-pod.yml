apiVersion: v1
kind: Pod
metadata:
  name: my-first-pod
  namespace: my-first-ns
  labels:
     app: myapp3
spec:
  containers:
  - name: my-first-container
    image: busybox
    command: ['sh', '-c', 'echo Hello buddy Kubernetes! && sleep 3600']

